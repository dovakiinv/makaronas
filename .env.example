# =============================================================================
# Makaronas Platform Configuration
# =============================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# The .env file must live at the project root (/path/to/makaronas/.env).
# Real environment variables take precedence over .env file values.
#
# Lines starting with # are comments. Variable values should not be quoted.
# =============================================================================


# -----------------------------------------------------------------------------
# AI Provider Keys
# -----------------------------------------------------------------------------
# API keys for AI model providers. Both are needed if you use models from both
# providers. Safe to leave blank for the provider you're not using.
#
# IMPORTANT: These are secrets. Never commit your .env file to version control.

# TEAM: Get your key from https://aistudio.google.com/apikey
# GOOGLE_API_KEY=your-google-api-key

# TEAM: Get your key from https://console.anthropic.com/
# ANTHROPIC_API_KEY=your-anthropic-api-key


# -----------------------------------------------------------------------------
# AI Backend Selection
# -----------------------------------------------------------------------------
# AI_BACKEND selects the default AI provider: "gemini" or "anthropic"
AI_BACKEND=gemini

# Each model variable takes a family name from backend/models.py.
# The config loader resolves family names to actual model IDs at startup.
#
# Valid family names:
#   CLAUDE_HAIKU       -> claude-haiku-4-5-20251001
#   CLAUDE_SONNET      -> claude-sonnet-4-6
#   CLAUDE_OPUS        -> claude-opus-4-6
#   GEMINI_FLASH_LITE  -> gemini-flash-lite-latest
#   GEMINI_FLASH       -> gemini-3-flash-preview
#   GEMINI_PRO         -> gemini-3-pro-preview
#
# To swap a model: change the value here, restart the server. One line changed.
TRICKSTER_MODEL=GEMINI_FLASH
COMPOSER_MODEL=CLAUDE_SONNET
EVALUATOR_MODEL=GEMINI_FLASH


# -----------------------------------------------------------------------------
# App Settings
# -----------------------------------------------------------------------------
# APP_ENV: "development", "staging", or "production"
# Affects logging verbosity and debug behaviour.
APP_ENV=development

# APP_PORT: The port uvicorn listens on.
APP_PORT=8000

# LOG_LEVEL: "debug", "info", "warning", or "error"
# Controls how much the server logs. Use "debug" during development,
# "info" or "warning" in production.
LOG_LEVEL=info

# CORS_ORIGINS: Comma-separated list of allowed frontend origins.
# Add your SPA dev server URL here. No trailing slashes.
CORS_ORIGINS=http://localhost:3000,http://localhost:5173


# -----------------------------------------------------------------------------
# Language
# -----------------------------------------------------------------------------
# DEFAULT_LANGUAGE: The language used when a student doesn't specify one.
# Lithuanian ("lt") is the primary language.
DEFAULT_LANGUAGE=lt

# SUPPORTED_LANGUAGES: Comma-separated list of enabled languages.
# Latvian ("lv") and English ("en") are planned additions.
SUPPORTED_LANGUAGES=lt


# -----------------------------------------------------------------------------
# Hooks (TEAM: configure as you replace stubs)
# -----------------------------------------------------------------------------
# The platform ships with in-memory stubs for all services. When the team
# swaps stubs for real implementations, uncomment and configure these.
#
# See backend/hooks/ for the interface each stub implements.

# Database connection for the real DatabaseAdapter implementation
# DATABASE_URL=postgresql://user:password@localhost:5432/makaronas

# Redis connection for session store or caching
# REDIS_URL=redis://localhost:6379/0

# Authentication provider configuration
# AUTH_PROVIDER=keycloak
# AUTH_ISSUER_URL=https://auth.example.com/realms/makaronas

# Cloud storage bucket for task assets (replaces LocalFileStorage)
# STORAGE_BUCKET=makaronas-assets
